function valenter(val){
    result.value+=val
}

function allclr(){
    result.value=''
}

function bckspace(){
    result.value=result.value.slice(0,-1)
    // data=result.value
    // s=data.slice(0,-1)
    //result.
}
function res(){
    result.value=eval(result.value)
}